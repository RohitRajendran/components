import {Preview, Props} from '@storybook/addon-docs/blocks';
import RadioButtonCard from './RadioButtonCard';

# RadioButtonCard

This component makes it easier to render a basic radio button card in a McGonagall flow. The collapsed state of this card uses the Simple Summary component. This card is typically used when there's only one valid answer to a question.

## Importing 📦

You can use this component using one of the following import patterns.

```tsx
import RadioButtonCard from '@unitedincome/components/dist/RadioButtonCard';
```

```tsx
import {RadioButtonCard} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <RadioButtonCard
    title="Choose an Option"
    description="There is only one"
    shortTitle="Answer"
    config={{
      value: 'yes',
      name: 'yesNo',
      options: [
        {
          label: 'Yes',
          value: 'yes',
        },
        {
          label: 'No',
          value: 'no',
        },
        {
          label: "I don't know",
          value: 'idk',
          disabled: true,
        },
      ],
    }}
  />
</Preview>

```tsx
<RadioButtonCard
  title="Choose an Option"
  description="There is only one"
  shortTitle="Answer"
  config={{
    value: 'yes',
    name: 'yesNo',
    options: [
      {
        label: 'Yes',
        value: 'yes',
      },
      {
        label: 'No',
        value: 'no',
      },
      {
        label: "I don't know",
        value: 'idk',
        disabled: true,
      },
    ],
  }}
/>
```

### Question Followup

If your answer requires a followup input or response, you can utilize the `followup` key in the `options` prop.

<Preview>
  <RadioButtonCard
    title="Choose an Option"
    description="There is only one"
    shortTitle="Answer"
    config={{
      value: 'yes',
      name: 'yesNo',
      options: [
        {
          label: 'Yes',
          value: 'yes',
          followup: <p>This is a followup!</p>,
        },
        {
          label: 'No',
          value: 'no',
        },
        {
          label: "I don't know",
          value: 'idk',
          disabled: true,
        },
      ],
    }}
  />
</Preview>

```tsx
<RadioButtonCard
  title="Choose an Option"
  description="There is only one"
  shortTitle="Answer"
  config={{
    value: 'yes',
    name: 'yesNo',
    options: [
      {
        label: 'Yes',
        value: 'yes',
      },
      {
        label: 'No',
        value: 'no',
      },
      {
        label: "I don't know",
        value: 'idk',
        disabled: true,
      },
    ],
  }}
/>
```

### Using in a McGonagall Flow

You can use the `RadioButtonCard` component in a McGonagall flow using the following pattern.

```tsx
export const renderCard = (props, state) => {
  return (
    <RadioButtonCard
      {...props}
      config={{
        value: 'yes',
        name: 'yesNo',
        options: [
          {
            label: 'Yes',
            value: 'yes',
          },
          {
            label: 'No',
            value: 'no',
          },
          {
            label: "I don't know",
            value: 'idk',
            disabled: true,
          },
        ],
      }}
    />
  );
};
```

## Props 🔧

<Props of={RadioButtonCard} />

## Related ✨

- [OptionBox](/?path=/story/atoms-optionbox--check)
- [RadioButtons](/?path=/story/molecules-radiobuttons--default)
- [McGonagall](/?path=/story/templates-mcgonagall--default)
