import {Preview, Props} from '@storybook/addon-docs/blocks';
import PaginatedTile from './PaginatedTile';

# PaginatedTile

Creates a Tile component that enables pagination. This allows the tile to render different views on separate pages.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import PaginatedTile from '@unitedincome/components/dist/PaginatedTile';
```

```javascript
import {PaginatedTile} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <PaginatedTile
    items={[
      'Monday',
      'Tuesday',
      'Wednesday',
      'Thursday',
      'Friday',
      'Saturday',
      'Sunday',
    ]}
    itemsPerPage={3}
    ListTemplate={({paginatedItems}) => {
      return (
        <ul>
          {paginatedItems.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      );
    }}
    tileProps={{
      footerContent: (
        <Button style={{textAlign: 'left'}} variant="link" to="#">
          View Montezuma the Cat
        </Button>
      ),
    }}
  />
</Preview>

```javascript
  <PaginatedTile
    items={[
    'Monday',
    'Tuesday',
    'Wednesday',
    'Thursday',
    'Friday',
    'Saturday',
    'Sunday',
  ]}
  itemsPerPage={3}
  ListTemplate={({paginatedItems}) => {
    return (
      <ul>
        {paginatedItems.map((item) => (
          <li>{item}</li>
        ))}
      </ul>
    );
  }}
  tileProps={{
    footerContent:
      <Button style={{textAlign: 'left'}} variant="link" to="#">
        View Montezuma the Cat
      </Button>
  }}
  />
  />
```

### No ListTemplate

The items rendered in the pagination tile can be different views as well.
In that case, embed the list of views(markup) in the `items` property and make sure to omit the `ListTemplate` property.
The `itemsPerPage` property can also be omitted as it becomes redundant in this case.

<Preview>
  <PaginatedTile
    items={[
      <React.Fragment>
        <p>
          This is a pagination example where the pages don't follow the same
          markup/template.
        </p>
      </React.Fragment>,
      <ul>
        {['Monday', 'Tuesday'].map((item) => (
          <li>{item}</li>
        ))}
      </ul>,
    ]}
    tileProps={{
      footerContent: (
        <Button style={{textAlign: 'left'}} variant="link" to="#">
          View Montezuma the Cat
        </Button>
      ),
    }}
  />
</Preview>

```javascript
<PaginatedTile
  items={[
    <React.Fragment>
      <p>
        This is a pagination example where the pages don't follow the same
        markup/template.
      </p>
    </React.Fragment>,
    <ul>
      {['Monday', 'Tuesday'].map((item) => (
        <li>{item}</li>
      ))}
    </ul>,
  ]}
  tileProps={{
    footerContent: (
      <Button style={{textAlign: 'left'}} variant="link" to="#">
        View Montezuma the Cat
      </Button>
    ),
  }}
/>
```

### Dark Mode

You can render the dark version of the paginated tile by passing in the `isDark` prop.

<Preview>
  <PaginatedTile
    isDark
    items={[
      'Monday',
      'Tuesday',
      'Wednesday',
      'Thursday',
      'Friday',
      'Saturday',
      'Sunday',
    ]}
    itemsPerPage={3}
    ListTemplate={({paginatedItems}) => {
      return (
        <ul>
          {paginatedItems.map((item) => (
            <li style={{color: 'inherit'}}>{item}</li>
          ))}
        </ul>
      );
    }}
    tileProps={{
      footerContent: (
        <Button style={{textAlign: 'left'}} variant="link" to="#">
          View Montezuma the Cat
        </Button>
      ),
    }}
  />
</Preview>

```javascript
<PaginatedTile
  items={[
    'Monday',
    'Tuesday',
    'Wednesday',
    'Thursday',
    'Friday',
    'Saturday',
    'Sunday',
  ]}
  itemsPerPage={3}
  ListTemplate={({paginatedItems}) => {
    return (
      <ul className="uic--body-text">
        {paginatedItems.map((item) => (
          <li style={{color: 'inherit'}}>{item}</li>
        ))}
      </ul>
    );
  }}
  tileProps={{
    footerContent: (
      <Button style={{textAlign: 'left'}} variant="link" to="#">
        View Montezuma the Cat
      </Button>
    ),
  }}
/>
```

## Props 🔧

<Props of={PaginatedTile} />

## Related ✨

- [Tile](/?path=/docs/atoms-tile--default)
