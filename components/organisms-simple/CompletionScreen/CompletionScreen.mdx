import {Preview, Props} from '@storybook/addon-docs/blocks';
import CompletionScreen from './CompletionScreen';
import LampCircleIllustration from '~components/atoms/illustrations/LampCircleIllustration/LampCircleIllustration.tsx';
import Button from '~components/atoms/Button/Button';

# CompletionScreen

This component renders the completion screen for the McGonagall flow. It should be the last screen in the flow.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import CompletionScreen from '@unitedincome/components/dist/CompletionScreen';
```

```javascript
import {CompletionScreen} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <CompletionScreen
    title="Montezuma is awesome"
    description="He's just so great!"
    feature={<LampCircleIllustration />}
    actions={
      <Button type="submit" variant="secondary" light>
        Finish
      </Button>
    }
  />
</Preview>

```javascript
<CompletionScreen
  title="Montezuma is awesome"
  description="He's just so great!"
  feature={<LampCircleIllustration />}
  actions={
    <Button type="submit" variant="secondary" light>
      Finish
    </Button>
  }
/>
```

### Using in a McGonagall Flow

You can use the `CompletionScreen` component in a McGonagall flow using the following pattern.

```javascript
export const renderCard = (props, state) => {
  return (
    <CompletionScreen
      {...props}
      actions={
        <Button type="submit" variant="secondary" light>
          Finish
        </Button>
      }
    />
  );
};
```

## Props 🔧

<Props of={CompletionScreen} />

## Related ✨

- [McGonagall](/?path=/story/templates-mcgonagall--default)
