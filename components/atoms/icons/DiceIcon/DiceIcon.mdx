import {Preview, Props} from '@storybook/addon-docs/blocks';
import DiceIcon from './DiceIcon.tsx';

# DiceIcon

The DiceIcon component creates a dice icon using inline SVG.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import DiceIcon from '@unitedincome/components/dist/DiceIcon';
```

```javascript
import {DiceIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <DiceIcon height="3rem" width="3rem" />
</Preview>

```javascript
<DiceIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <DiceIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<DiceIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={DiceIcon} />
