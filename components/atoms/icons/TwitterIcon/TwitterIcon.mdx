import {Preview, Props} from '@storybook/addon-docs/blocks';
import TwitterIcon from './TwitterIcon.tsx';

# TwitterIcon

The TwitterIcon component creates a Twitter social media icon using inline SVG. This icon is typically used within page/content footers and headers.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import TwitterIcon from '@unitedincome/components/dist/TwitterIcon';
```

```javascript
import {TwitterIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <TwitterIcon height="3rem" width="3rem" fill="#000" />
</Preview>

```javascript
<TwitterIcon height="3rem" width="3rem" fill="#000" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <TwitterIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<TwitterIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={TwitterIcon} />
