import {Preview, Props} from '@storybook/addon-docs/blocks';
import OptionBox from './OptionBox';

# OptionBox

The OptionBox component displays an option box which can be used in either select or radio button inputs.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import OptionBox from '@unitedincome/components/dist/OptionBox';
```

```javascript
import {OptionBox} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <OptionBox variant="check" checked={true} disabled={false} />
</Preview>

```javascript
<OptionBox variant="check" checked={true} disabled={false} />
```

### Variant

You can provide either `check` or `radio` to the `variant` prop to change the type of option box.

<Preview>
  <OptionBox variant="radio" checked={true} disabled={false} />
</Preview>

```javascript
<OptionBox variant="radio" checked={true} disabled={false} />
```

## Props 🔧

<Props of={OptionBox} />

## Related ✨

- [Checkboxes](/?path=/story/molecules-checkboxes--default)
- [RadioButtons](/?path=/story/molecules-radiobuttons--default)
