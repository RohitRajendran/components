import {Preview, Props} from '@storybook/addon-docs/blocks';
import ThumbsUpIcon from './ThumbsUpIcon.tsx';

# ThumbsUpIcon

The ThumbsUpIcon component creates a thumbs up icon using inline SVG.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import ThumbsUpIcon from '@unitedincome/components/dist/ThumbsUpIcon';
```

```javascript
import {ThumbsUpIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <ThumbsUpIcon height="3rem" width="3rem" />
</Preview>

```javascript
<ThumbsUpIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <ThumbsUpIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<ThumbsUpIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={ThumbsUpIcon} />
