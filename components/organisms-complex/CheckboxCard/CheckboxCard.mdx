import {Preview, Props} from '@storybook/addon-docs/blocks';
import CheckboxCard from './CheckboxCard';

# CheckboxCard

This component makes it easier to render a basic radio button card in a McGonagall flow. The collapsed state of this card uses the Simple Summary component. This card is typically used when there's only one valid answer to a question.

## Importing 📦

You can use this component using one of the following import patterns.

```tsx
import CheckboxCard from '@unitedincome/components/dist/CheckboxCard';
```

```tsx
import {CheckboxCard} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <CheckboxCard
    title="Choose an Option"
    description="There is only one"
    shortTitle="Answer"
    config={{
      name: 'selectBtn',
      value: [],
      options: [
        {
          label: 'Yes',
          value: 'yes',
        },
        {
          label: 'No',
          value: 'no',
        },
        {
          label: "I don't know",
          value: 'idk',
          disabled: true,
        },
      ],
    }}
  />
</Preview>

```tsx
<CheckboxCard
  title="Choose an Option"
  description="There is only one"
  shortTitle="Answer"
  config={{
    name: 'selectBtn',
    value: []
    options: [
      {
        label: 'Yes',
        value: 'yes',
      },
      {
        label: 'No',
        value: 'no',
      },
      {
        label: "I don't know",
        value: 'idk',
        disabled: true,
      },
    ],
  }}
/>
```

### Rendering in a Table

If you'd like to render the checkboxes in a table, you can provide `table: true` to the `config` prop.

<Preview>
  <CheckboxCard
    title="Choose an Option"
    description="There is only one"
    shortTitle="Answer"
    config={{
      name: 'selectBtn',
      table: true,
      value: [],
      options: [
        {
          label: 'Yes',
          value: 'yes',
        },
        {
          label: 'No',
          value: 'no',
        },
        {
          label: "I don't know",
          value: 'idk',
          disabled: true,
        },
      ],
    }}
  />
</Preview>

```tsx
<CheckboxCard
  title="Choose an Option"
  description="There is only one"
  shortTitle="Answer"
  config={{
    name: 'selectBtn',
    table: true,
    value: []
    options: [
      {
        label: 'Yes',
        value: 'yes',
      },
      {
        label: 'No',
        value: 'no',
      },
      {
        label: "I don't know",
        value: 'idk',
        disabled: true,
      },
    ],
  }}
/>
```

### Using in a McGonagall Flow

You can use the `CheckboxCard` component in a McGonagall flow using the following pattern.

```tsx
export const renderCard = (props, state) => {
  return (
    <CheckboxCard
      {...props}
      config={{
        name: 'selectBtn',
        value: 'yes',
        options: [
          {
            label: 'Yes',
            value: 'yes',
          },
          {
            label: 'No',
            value: 'no',
          },
          {
            label: "I don't know",
            value: 'idk',
            disabled: true,
          },
        ],
      }}
    />
  );
};
```

## Props 🔧

<Props of={CheckboxCard} />

## Related ✨

- [OptionBox](/?path=/story/atoms-optionbox--check)
- [Checkboxes](/?path=/story/molecules-checkboxes--default)
- [McGonagall](/?path=/story/templates-mcgonagall--default)
