import {Preview, Props} from '@storybook/addon-docs/blocks';
import ArrowEllipsisIcon from './ArrowEllipsisIcon.tsx';

# ArrowEllipsisIcon

The ArrowEllipsisIcon component creates an arrow with a line icon using inline SVG.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import ArrowEllipsisIcon from '@unitedincome/components/dist/ArrowEllipsisIcon';
```

```javascript
import {ArrowEllipsisIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <ArrowEllipsisIcon height="3rem" width="3rem" />
</Preview>

```javascript
<ArrowEllipsisIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <ArrowEllipsisIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<ArrowEllipsisIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={ArrowEllipsisIcon} />
