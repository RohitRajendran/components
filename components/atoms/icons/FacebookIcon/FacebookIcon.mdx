import {Preview, Props} from '@storybook/addon-docs/blocks';
import FacebookIcon from './FacebookIcon.tsx';

# FacebookIcon

The FacebookIcon component creates a LinkedIn social media icon using inline SVG. This icon is typically used within page/content footers and headers.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import FacebookIcon from '@unitedincome/components/dist/FacebookIcon';
```

```javascript
import {FacebookIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <FacebookIcon height="3rem" width="3rem" fill="#000" />
</Preview>

```javascript
<FacebookIcon height="3rem" width="3rem" fill="#000" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <FacebookIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<FacebookIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={FacebookIcon} />
