import {Preview, Props} from '@storybook/addon-docs/blocks';
import PrinterCircleIcon from './PrinterCircleIcon.tsx';

# PrinterCircleIcon

The PrinterCircleIcon component creates a Printer social media icon using inline SVG.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import PrinterCircleIcon from '@unitedincome/components/dist/PrinterCircleIcon';
```

```javascript
import {PrinterCircleIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <PrinterCircleIcon height="3rem" width="3rem" fill="#000" />
</Preview>

```javascript
<PrinterCircleIcon height="3rem" width="3rem" fill="#000" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <PrinterCircleIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<PrinterCircleIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={PrinterCircleIcon} />
