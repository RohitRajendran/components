import {Preview, Props} from '@storybook/addon-docs/blocks';
import InputCard from './InputCard';

# InputCard

This component makes it easier to render a basic radio button card in a McGonagall flow. The collapsed state of this card uses the Simple Summary component. This card is typically used when there's only one valid answer to a question.

## Importing 📦

You can use this component using one of the following import patterns.

```tsx
import InputCard from '@unitedincome/components/dist/InputCard';
```

```tsx
import {InputCard} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <InputCard
    title="Choose an Option"
    description="There is only one"
    shortTitle="Answer"
    config={{
      name: 'input',
      value: 'Montezuma',
      label: 'Who is the best cat?',
      type: 'text',
    }}
  />
</Preview>

```tsx
<InputCard
  title="Choose an Option"
  description="There is only one"
  shortTitle="Answer"
  config={{
    name: 'input',
    value: 'Montezuma',
    label: 'Who is the best cat?',
    type: 'text',
  }}
/>
```

### Using in a McGonagall Flow

You can use the `InputCard` component in a McGonagall flow using the following pattern.

```tsx
export const renderCard = (props, state) => {
  return (
    <InputCard
      {...props}
      config={{
        name: 'input',
        value: state.input,
        label: 'value',
        type: 'text',
      }}
    />
  );
};
```

## Props 🔧

<Props of={InputCard} />

## Related ✨

- [Input](/?path=/story/atoms-input--default)
- [McGonagall](/?path=/story/templates-mcgonagall--default)
