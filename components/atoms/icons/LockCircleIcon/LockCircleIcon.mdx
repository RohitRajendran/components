import {Preview, Props} from '@storybook/addon-docs/blocks';
import LockCircleIcon from './LockCircleIcon';

# LockCircleIcon

The LockCircleIcon component creates a lock icon using inline SVG. This icon is typically used to indicate that the content is locked and an action is required to access it.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import LockCircleIcon from '@unitedincome/components/dist/LockCircleIcon';
```

```javascript
import {LockCircleIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <LockCircleIcon />
</Preview>

```javascript
<LockCircleIcon />
```

### Changing the Color

You can adjust the color of the icon using the `fill` and `outerFill` props.

<Preview>
  <LockCircleIcon fill="#ababab" outerFill="#123" />
</Preview>

```javascript
<LockCircleIcon fill="#ababab" outFill="#123" />
```

## Props 🔧

<Props of={LockCircleIcon} />
