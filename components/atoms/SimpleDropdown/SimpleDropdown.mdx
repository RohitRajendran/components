import {Preview, Props} from '@storybook/addon-docs/blocks';
import SimpleDropdown from './SimpleDropdown';

# SimpleDropdown

The SimpleDropdown component renders a basic static dropdown menu.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import SimpleDropdown from '@unitedincome/components/dist/SimpleDropdown';
```

```javascript
import {SimpleDropdown} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <SimpleDropdown
    options={['montezuma1', 'montezuma2', 'montezuma3']}
    value="montezuma1"
    onChange={(value) => alert(value)}
  />
</Preview>

```javascript
<SimpleDropdown
  options={['montezuma1', 'montezuma2', 'montezuma3']}
  value="montezuma1"
  onChange={(value) => alert(value)}
/>
```

## Props 🔧

<Props of={SimpleDropdown} />

## Related ✨

- [DropDown](/?path=/story/atoms-dropdown--default)
