import {Preview, Props} from '@storybook/addon-docs/blocks';
import CheckIcon from './CheckIcon.tsx';

# CheckIcon

The CheckIcon component creates a checkmark icon using inline SVG.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import CheckIcon from '@unitedincome/components/dist/CheckIcon';
```

```javascript
import {CheckIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <CheckIcon height="3rem" width="3rem" />
</Preview>

```javascript
<CheckIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <CheckIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<CheckIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={CheckIcon} />
