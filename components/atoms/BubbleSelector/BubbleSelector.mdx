import {Preview, Props} from '@storybook/addon-docs/blocks';
import BubbleSelector from './BubbleSelector.tsx';

# BubbleSelector

The BubbleSelector component renders a horizontal item selector in a bubble style.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import BubbleSelector from '@unitedincome/components/dist/BubbleSelector';
```

```javascript
import {BubbleSelector} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <BubbleSelector
    options={[
      {value: 'montezuma1'},
      {value: 'montezuma2'},
      {value: 'montezuma3'},
    ]}
    value="montezuma1"
    onChange={(value) => true}
  />
</Preview>

```javascript
<BubbleSelector
  options={[
    {value: 'montezuma1'},
    {value: 'montezuma2'},
    {value: 'montezuma3'},
  ]}
  value="montezuma1"
  onChange={(value) => true}
/>
```

### Bubble Colors

You can change each bubbles color by providing the option with a `color` property.

<Preview>
  <BubbleSelector
    options={[
      {value: 'montezuma1'},
      {value: 'montezuma2'},
      {value: 'montezuma3', color: 'Red'},
    ]}
    value="montezuma3"
    onChange={(value) => true}
  />
</Preview>

```javascript
<BubbleSelector
  options={[
    {value: 'montezuma1'},
    {value: 'montezuma2'},
    {value: 'montezuma3', color: 'Red'},
  ]}
  value="montezuma3"
  onChange={(value) => true}
/>
```

### Adjusting the Width

If you'd like to change the width of the bubbles, you can provide the component with a `fixedWidth` prop.

<Preview>
  <BubbleSelector
    fixedWidth="14rem"
    options={[
      {value: 'montezuma1'},
      {value: 'montezuma2'},
      {value: 'montezuma3', color: 'Red'},
    ]}
    value="montezuma3"
    onChange={(value) => true}
  />
</Preview>

```javascript
<BubbleSelector
  fixedWidth="14rem"
  options={[
    {value: 'montezuma1'},
    {value: 'montezuma2'},
    {value: 'montezuma3', color: 'Red'},
  ]}
  value="montezuma3"
  onChange={(value) => true}
/>
```

## Props 🔧

<Props of={BubbleSelector} />

## Related ✨

- [BubbleNav](/?path=/story/molecules-bubblenav--default)
- [BubbleDropdown](/?path=/docs/atoms-bubbledropdown--default)
