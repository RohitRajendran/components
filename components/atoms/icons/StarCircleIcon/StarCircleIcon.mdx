import {Preview, Props} from '@storybook/addon-docs/blocks';
import StarCircleIcon from './StarCircleIcon.tsx';

# StarCircleIcon

The StarCircleIcon component creates a star icon using inline SVG. This icon is typically used to indidicate reccomendations.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import StarCircleIcon from '@unitedincome/components/dist/StarCircleIcon';
```

```javascript
import {StarCircleIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <StarCircleIcon height="3rem" width="3rem" />
</Preview>

```javascript
<StarCircleIcon height="3rem" width="3rem" />
```

### Changing the Colors

You can adjust the color of the icon using the `fill` and `outerFill` props. These prop accepts common color names and hex values.

<Preview>
  <StarCircleIcon height="3rem" width="3rem" fill="blue" outerFill="orange" />
</Preview>

```javascript
<StarCircleIcon height="3rem" width="3rem" fill="blue" outerFill="orange" />
```

## Props 🔧

<Props of={StarCircleIcon} />
