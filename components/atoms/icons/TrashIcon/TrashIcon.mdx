import {Preview, Props} from '@storybook/addon-docs/blocks';
import TrashIcon from './TrashIcon.tsx';

# TrashIcon

The TrashIcon component creates a trash can icon using inline SVG. This icon is typically used to represent a delete action.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import TrashIcon from '@unitedincome/components/dist/TrashIcon';
```

```javascript
import {TrashIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <TrashIcon height="3rem" width="3rem" />
</Preview>

```javascript
<TrashIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <TrashIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<TrashIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={TrashIcon} />
