import {Preview, Props} from '@storybook/addon-docs/blocks';
import LinkedInIcon from './LinkedInIcon.tsx';

# LinkedInIcon

The LinkedInIcon component creates a LinkedIn social media icon using inline SVG. This icon is typically used within page/content footers and headers.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import LinkedInIcon from '@unitedincome/components/dist/LinkedInIcon';
```

```javascript
import {LinkedInIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <LinkedInIcon height="3rem" width="3rem" fill="#000" />
</Preview>

```javascript
<LinkedInIcon height="3rem" width="3rem" fill="#000" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <LinkedInIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<LinkedInIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={LinkedInIcon} />
