import {Preview, Props} from '@storybook/addon-docs/blocks';
import ClearIcon from './ClearIcon.tsx';

# ClearIcon

The ClearIcon component creates an x icon using inline SVG. This icon is typically used to represent that content can be closed/supresed.

## Importing 📦

You can use this component using one of the following import patterns.

```javascript
import ClearIcon from '@unitedincome/components/dist/ClearIcon';
```

```javascript
import {ClearIcon} from '@unitedincome/components';
```

## Example 🚀

<Preview>
  <ClearIcon height="3rem" width="3rem" />
</Preview>

```javascript
<ClearIcon height="3rem" width="3rem" />
```

### Changing the Color

You can adjust the color of the icon using the `fill` prop. This prop accepts common color names and hex values.

<Preview>
  <ClearIcon height="3rem" width="3rem" fill="#ababab" />
</Preview>

```javascript
<ClearIcon height="3rem" width="3rem" fill="#ababab" />
```

## Props 🔧

<Props of={ClearIcon} />
