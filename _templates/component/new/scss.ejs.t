---
to: components/<%= compPath %>/<%= h.changeCase.pascal(name) %>/<%= h.changeCase.pascal(name) %>.scss
---
.uic--<%= h.changeCase.paramCase(name) %> {
  // Review the README for info on how to namespace css classes
  // Delete this file and the import in the js if the component doesn't need any custom stylings
  // https://github.com/UnitedIncome/components#best-practices-
}
